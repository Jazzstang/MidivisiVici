#include "DivisiDropVoicing.h"

DivisiDropVoicing::DivisiDropVoicing(juce::AudioProcessorValueTreeState& state)
    : parameters(state)
{
    // Initialisation des composants spécifiques
}

DivisiDropVoicing::~DivisiDropVoicing() = default;

void DivisiDropVoicing::resized()
{
    // Gestion des positions de composants si nécessaire
}
